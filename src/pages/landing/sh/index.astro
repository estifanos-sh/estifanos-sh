---
import SiteHeader from "../../../components/SiteHeader.astro";
import LandingLayout from "../../../layouts/LandingLayout.astro";
import configuration from "../../../../config/docs.json";

const projects = configuration.projects.filter((project) => project.published);
---

<LandingLayout
  description="Open-source infrastructure developed and maintained by Estifanos Engineering."
  favicon="/estifanos-mark-green.svg"
  site="sh"
  title="estifanos.sh"
>
  <div class="site">
    <SiteHeader site="sh" />
    <main class="profile projects-profile">
      <p class="engineering-intro">Estifanos Engineering develops and maintains open-source software and SDKs.</p>
      <h1>Current Projects</h1>
      <ul class="project-links">
        {projects.map((project) => <li><a href={project.mountPath}>@{project.id}</a></li>)}
      </ul>
    </main>
  </div>
</LandingLayout>
